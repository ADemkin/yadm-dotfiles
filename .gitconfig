[user]
    name = "demkin.anton"
    email = "demkin.anton@rwb.ru"
[alias]
    ch = checkout
    co = checkout
    com = checkout origin/main
    chb = checkout -b
    cob = checkout -b
    st = status
    graph = log --graph --date-order --pretty=format:\"%h %ad %Cgreen%d%Creset %s [%an]\" --date=short --all
    retag = tag -f
    ru = remote update
    rb = rebase
    rbi = rebase -i
    rbim = rebase -i origin/main
    br = "!git branch --show-current 2>/dev/null"
    fa = "!git add $(git ls-files --modified --deleted --other --exclude-standard --deduplicate | fzf --multi --reverse --no-sort --height=40 --border --border-label=' git add ' --info-command='echo C-j/k to move, C-a to select, C-f to select all' --border-label-pos=4 --highlight-line --bind=ctrl-a:toggle,ctrl-f:toggle-all --preview='git diff --color=always {+} | delta --hunk-header-style=omit') 2>/dev/null"
    brcl = "!git branch --merged | grep -Ev '(^\\*|^\\+|master|main|dev|stage|staging)' | xargs -n 1 git branch -d"
    changes = "!git log --format=format: --name-only --since='1 year ago' | sort | uniq -c | sort -nr | head -20"
    who = "!git shortlog -sn --no-merges"
    problems = "!git log -i -E --grep='fix|bug|broken' --name-only --format='' | sort | uniq -c | sort -nr | head -20"
    changes-per-month = "!git log --format='%ad' --date=format:'%Y-%m' | sort | uniq -c"
[core]
    excludesfile = ~/.gitignore
    ; sshCommand = "ssh -i ~/.ssh/id_ed25519 -F /dev/null"
[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
[push]
    autoSetupRemote = true
;
; delta config
[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only --features=interactive

[delta]
    features = decorations
    ; syntax-theme = gruvbox-dark
    syntax-theme = Monokai Extended
    ; minus-style = syntax "#330011"
    ; plus-style = syntax "#0c2705"

[delta "interactive"]
    keep-plus-minus-markers = false

[delta "decorations"]
    commit-decoration-style = blue ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = yellow ol
    hunk-header-file-style = red
    hunk-header-style = file line-number

; wb
[includeIf "hasconfig:remote.*.url:git@*.wildberries.ru*/**"]
    path = .gitconfig-wb
